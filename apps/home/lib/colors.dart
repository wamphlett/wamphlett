import 'package:flutter/material.dart';

// The main colors that we will be using
class BrandColors {
  BrandColors._();
  static const Color primary = Color(0xFF67BBC2);
  static const Color grey = Color(0xFFEBEBEB);
}