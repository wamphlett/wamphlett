import { buildEvent } from '../data';
import { EventType } from '../types';

export const Japan25 = buildEvent({
  type: EventType.Travel,
  year: 2025,
  month: 1,
  intro: 'Japan',
  title: 'Tokyo, Fukushima & Hokkaido',
  tagline: 'First of three this year',
  images: [
    
  ],
});
